<% if (!events || events.length === 0) { %>
  <p>No archived events yet.</p>
<% } else { %>
  <ul>
    <% events.forEach((event) => { %>
      <li>
        <strong><%= event.title %></strong><br />
        <span><%= event.location %></span><br />
        <span>Ended: <%= event.endTime %></span><br />
        <span>Status: <%= event.status %></span>
      </li>
    <% }) %>
  </ul>
<% } %>